apply plugin: 'groovy'
apply plugin: 'maven'

dependencies {
    compile gradleApi()
    compile localGroovy()
    compile 'com.android.tools.build:gradle:2.2.2'
    compile 'org.javassist:javassist:3.21.0-GA'
}

repositories {
    jcenter()
}

group='com.loongee.logger'
version='0.0.1'

uploadArchives {
    repositories {
        mavenLocal()
    }
}
